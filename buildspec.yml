
version: 0.2

phases:
  pre_build:
    commands:
      - echo "Iniciando fase de pre-construcción..."
      - echo "Iniciando sesión en Amazon ECR..."
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 253490770873.dkr.ecr.us-east-2.amazonaws.com
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 253490770873.dkr.ecr.us-east-2.amazonaws.com
  build:
    commands:
      - echo "Fase de construcción iniciada en `date`"
      - echo "Construyendo la imagen Docker..."
      - docker build -t nr-registry-test .
      - echo "Etiquetando la imagen Docker..."
      - docker tag nr-registry-test:latest 253490770873.dkr.ecr.us-east-2.amazonaws.com/nr-registry-test:latest
  post_build:
    commands:
      - echo "Fase de post-construcción iniciada en `date`"
      - echo "Empujando la imagen Docker al Amazon ECR..."
      - docker tag nr-registry-test:latest 253490770873.dkr.ecr.us-east-2.amazonaws.com/nr-registry-test:latest
      - echo "Imagen empujada exitosamente"
artifacts:
  files:
    - '**/*'
  discard-paths: yes

